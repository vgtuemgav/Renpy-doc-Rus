.. Automatically generated file - do not modify.

.. class:: Gallery()
    
    Этот класс поддерживает создание галереи изображений, управляя
    блокировкой изображений, предоставляя действие, которое может показывать
    одно или несколько изображений, и метод, создающий кнопки,
    использующие это действие.
    
    .. attribute:: enter_transition
    
        Переход, используемый при отображении первого изображения,
        связанного с кнопкой галереи.
    
    .. attribute:: intra_transition
    
        Переход, используемый при отображении изображений, связанных
        с кнопками галереи, кроме первого.
    
    .. attribute:: exit_transition
    
        Переход, используемый при возвращении с последнего изображения,
        связанного с кнопкой галереи, на экран галереи.
    
    .. attribute:: transition
    
        Используется вместо enter_transition, intra_transition или
        exit_transition, если один или несколько из них не установлены.
    
    .. attribute:: locked_button
    
        Отображаемый объект по умолчанию, используемый make_button для
        заблокированной кнопки.
    
    .. attribute:: hover_border
    
        Рамка при наведении по умолчанию, используемая make_button.
    
    .. attribute:: idle_border
    
        Рамка в состоянии покоя по умолчанию, используемая make_button.
    
    .. attribute:: unlocked_advance
    
        Если true, галерея будет переключаться только между
        разблокированными изображениями.
    
    .. attribute:: navigation
    
        Если true, галерея будет отображать кнопки навигации и
        слайд-шоу поверх изображений.
        
        Чтобы настроить внешний вид навигации, вы можете переопределить
        экран gallery_navigation. Экран по умолчанию определён
        в renpy/common/00gallery.rpy
    
    .. attribute:: span_buttons
    
        Если true, галерея будет переключаться между кнопками.
    
    .. attribute:: slideshow_delay
    
        Время, через которое галерея будет переключаться между изображениями
        в режиме слайд-шоу.
    
    .. attribute:: image_screen = "_gallery"
    
        Экран, который используется для показа отдельных изображений в
        этой галерее. Этому экрану передаются следующие именованные
        аргументы:
    
        `locked`
            True, если изображение заблокировано.
        `displayables`
            Список преобразованных отображаемых объектов, которые
            должны быть показаны пользователю.
        `index`
            Индекс отображаемого изображения, начиная с 1.
        `count`
            Количество изображений, привязанных к текущей кнопке.
        `gallery`
            Объект галереи изображений.
    
        Дополнительные аргументы могут быть переданы путем добавления к
        ним префикса `show_` в вызовах Gallery.image и Gallery.unlock_image.
    
        Экран по умолчанию определён в конце файла renpy/common/00gallery.rpy.

    .. method:: Action(name)
        
        Действие, которое отображает изображения, связанные с указанным
        именем кнопки.
    
    .. method:: Next(unlocked=False)
        
        Переходит к следующему изображению в галерее.
        
        `unlocked`
            Если true, учитывает только разблокированные изображения.
    
    .. method:: Previous(unlocked=False)
        
        Переходит к предыдущему изображению в галерее.
        
        `unlocked`
            Если true, учитывает только разблокированные изображения.
    
    .. method:: Return()
        
        Прекращает показ изображений галереи.
    
    .. method:: ToggleSlideshow()
        
        Переключает режим слайд-шоу.
    
    .. method:: allprior()
        
        Условие, которое истинно, если все предыдущие изображения,
        связанные с текущей кнопкой, были разблокированы.
    
    .. method:: button(name)
        
        Создаёт новую кнопку с именем `name`.
        
        `name`
            Имя создаваемой кнопки.
    
    .. method:: condition(expression)
        
        Условие, которое выполняется, когда выражение оценивается
        как истинное.
        
        `expression`
            Строка, содержащая выражение Python.
    
    .. method:: get_fraction(name, format='{seen}/{total}')
        
        Возвращает текстовую строку с количеством разблокированных
        изображений и общим количеством изображений в кнопке с именем
        `name`. Если `name` равно None, возвращает ту же информацию
        для всех кнопок.
        
        `format`
            Строка форматирования Python, используемая для форматирования
            чисел. В неё можно подставить три значения:
        
            {seen}
                Количество просмотренных изображений.
            {total}
                Общее количество изображений в кнопке.
            {locked}
                Количество изображений, которые всё ещё заблокированы.
    
    .. method:: image(*displayables, **properties)
        
        Добавляет новое изображение к текущей кнопке, где изображение
        состоит из одного или нескольких отображаемых объектов (displayables).
        
        У свойств, начинающихся с `show_`, этот префикс отбрасывается,
        и они передаются экрану gallery.image_screen в качестве
        дополнительных именованных аргументов.
        
        Принимает один именованный аргумент:
        
        `movie`
            Следует установить в true, если один из отображаемых объектов
            является видео со звуком. Это приведёт к тому, что видео
            будет скрыто во время перехода.
    
    .. method:: make_button(name, unlocked, locked=None, hover_border=None, idle_border=None, style=None, **properties)
        
        Создаёт кнопку, которая отображает изображения, связанные
        с указанным именем кнопки.
        
        `name`
            Имя создаваемой кнопки.
        
        `unlocked`
            Отображаемый объект, который показывается для этой кнопки,
            когда она разблокирована.
        
        `locked`
            Отображаемый объект, который показывается для этой кнопки,
            когда она заблокирована. Если None, вместо него используется
            поле locked_button объекта галереи.
        
        `hover_border`
            Отображаемый объект, который накладывается на эту кнопку,
            когда она разблокирована и находится в фокусе. Если None,
            используется поле hover_border объекта галереи.
        
        `idle_border`
            Отображаемый объект, который накладывается на эту кнопку,
            когда она разблокирована, но не в фокусе. Если None,
            используется поле idle_border объекта галереи.
        
        `style`
            Стиль, который наследует кнопка. Если None, по умолчанию
            используется стиль "empty", чтобы не наследовать рамки
            и т.д.
        
        Дополнительные именованные аргументы становятся свойствами
        стиля созданного объекта кнопки.
    
    .. method:: transform(*transforms)
        
        Применяет трансформации к последнему зарегистрированному
        изображению. Следует вызывать с таким же количеством трансформаций,
        сколько отображаемых объектов у изображения. Трансформации
        применяются к соответствующим отображаемым объектам.
        
        Если трансформация равна None, используется трансформация
        по умолчанию.
    
    .. method:: unlock(*images)
        
        Условие, которое принимает одно или несколько имён изображений
        в качестве аргумента и выполняется, когда все указанные
        изображения были просмотрены игроком. Имена изображений должны
        быть строками.
    
    .. method:: unlock_image(*images, **properties)
        
        Удобный метод, эквивалентный вызову image и unlock с теми
        же параметрами. (Именованные аргументы, начинающиеся с
        ``show_``, передаются только в image.) Это приведёт к
        отображению изображения, если оно было просмотрено ранее.
        
        Изображения следует указывать в виде строк, содержащих
        их имена.