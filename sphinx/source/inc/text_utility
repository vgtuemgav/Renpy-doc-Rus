.. Automatically generated file - do not modify.

.. function:: renpy.filter_text_tags(s, allow=None, deny=None)
    
    Возвращает копию `s` с отфильтрованными тегами текста. Должен быть указан ровно один из именованных
    аргументов `allow` или `deny`.
    
    `allow`
        Множество тегов, которые разрешены. Если тег отсутствует в этом списке, он удаляется.
    
    `deny`
        Множество тегов, которые запрещены. Если тег отсутствует в этом списке, он остается в строке.

.. function:: renpy.substitute(s, scope=None, translate=True)
    
    Применяет перевод и форматирование нового стиля к строке `s`.
    
    `scope`
        Если не None, это область видимости (scope), которая используется при форматировании, в дополнение
        к стандартному хранилищу.
    
    `translate`
        Определяет, происходит ли перевод.
    
    Возвращает переведенную и отформатированную строку.

.. function:: renpy.transform_text(s, function)
    
    Преобразует `s`, оставляя текстовые теги и интерполяцию без изменений.
    
    `function`
        Функция, которая вызывается со строками, соответствующими фрагментам
        текста, и должна возвращать другую строку, которая заменяет этот фрагмент
        текста.
    
    ::
    
        init python:
            def upper(s):
                return s.upper()
    
        $ upper_string = renpy.transform_text("{b}Not Upper{/b}", upper)