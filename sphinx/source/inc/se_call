.. Automatically generated file - do not modify.

.. function:: renpy.call(label, *args, from_current=False, **kwargs)
    
    Приводит к завершению текущего оператора Ren'Py и переходу к
    метке `label`. Когда переход вернется, управление будет передано
    оператору, следующему за текущим.
    
    Метка должна иметь форму "global_name" или "global_name.local_name".
    Форма ".local_name" не допускается.
    
    `from_current`
        Если true, управление вернется к текущему оператору, а не
        к оператору, следующему за текущим. (Это приведет к
        тому, что текущий оператор будет выполнен дважды. Этот параметр должен передаваться как
        именованный аргумент.)

.. function:: renpy.return_statement(value=None)
    
    Приводит к тому, что Ren'Py возвращается из текущего вызова уровня Ren'Py.