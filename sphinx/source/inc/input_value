.. Automatically generated file - do not modify.

.. function:: DictInputValue(dict, key, default=True, returnable=False)
    
    Значение ввода, которое обновляет ``dict[key]``.
    
    `dict`
        Может быть объектом dict или списком.
    `default`
        Если истинно, это поле ввода может быть редактируемым по умолчанию.
    `returnable`
        Если истинно, значение этого поля ввода будет возвращено, когда
        пользователь нажмёт enter.

.. function:: FieldInputValue(object, field, default=True, returnable=False)
    
    Значение ввода, которое обновляет поле `field` в объекте `object`.
    
    `field`
        Строка, задающая имя поля.
    `default`
        Если истинно, это поле ввода может быть редактируемым по умолчанию.
    `returnable`
        Если истинно, значение этого поля ввода будет возвращено, когда
        пользователь нажмёт enter.

.. function:: FilePageNameInputValue(pattern='Page {}', auto='Automatic saves', quick='Quick saves', page=None, default=False)
    
    Значение ввода, которое обновляет имя страницы сохранения файла.
    
    `pattern`
        Используется для имени страницы по умолчанию. Выполняется подстановка
        в стиле Python, так что {} заменяется номером страницы.
    
    `auto`
        Имя страницы автосохранения.
    
    `quick`
        Имя страницы быстрого сохранения.
    
    `page`
        Если указан, номер отображаемой страницы. Обычно это следует
        оставлять как None, чтобы использовать текущую страницу.
    
    `default`
        Если истинно, это поле ввода может быть редактируемым по умолчанию.

.. function:: LocalVariableInputValue(variable, default=True, returnable=False)
    
    Значение ввода, которое обновляет локальную переменную в экране,
    используемом через ``use``.
    
    Для обращения к переменной в экране верхнего уровня, предпочтительнее
    использовать :func:`ScreenVariableInputValue`.
    
    Для получения дополнительной информации см. :ref:`sl-use`.
    
    Этот объект должен быть создан в контексте, где установлена переменная —
    его нельзя передать из другого места.
    
    `variable`
        Строка, задающая имя обновляемой переменной.
    `default`
        Если истинно, это поле ввода может быть редактируемым по умолчанию.
    `returnable`
        Если истинно, значение этого поля ввода будет возвращено, когда
        пользователь нажмёт enter.

.. function:: ScreenVariableInputValue(variable, default=True, returnable=False)
    
    Значение ввода, которое обновляет переменную в экране.
    
    В экране, используемом через ``use``, это обращается к переменной
    в контексте экрана, содержащего используемый экран(ы). Для обращения к
    переменным внутри экрана, используемого через ``use``, и только в этом случае,
    используйте :func:`LocalVariableInputValue`.
    
    `variable`
        Строка, задающая имя обновляемой переменной.
    `default`
        Если истинно, это поле ввода может быть редактируемым по умолчанию.
    `returnable`
        Если истинно, значение этого поля ввода будет возвращено, когда
        пользователь нажмёт enter.

.. function:: VariableInputValue(variable, default=True, returnable=False)
    
    Значение ввода, которое обновляет `variable`.
    
    `variable`
        Строка, задающая имя обновляемой переменной.
    
        Параметр `variable` должен быть строкой и может быть простым именем,
        например, "strength", или именем с точками, разделяющими переменную
        и поля, например, "hero.strength" или "persistent.show_cutscenes".
    `default`
        Если истинно, это поле ввода может быть редактируемым по умолчанию.
    `returnable`
        Если истинно, значение этого поля ввода будет возвращено, когда
        пользователь нажмёт enter.