.. Automatically generated file - do not modify.

.. function:: renpy.register_shader(name, **kwargs)
    
    Регистрирует часть шейдера. Принимает `name`, а затем
    именованные аргументы.
    
    `name`
        Строка, задающая имя части шейдера. Имена, начинающиеся с
        подчёркивания или "renpy.", зарезервированы для Ren'Py.
    
    `variables`
        Переменные, используемые частью шейдера. Они должны быть перечислены по одной на
        строку: спецификатор хранения (uniform, attribute или varying), за которым следуют тип,
        имя и точка с запятой. Например::
    
            variables='''
            uniform sampler2D tex0;
            attribute vec2 a_tex_coord;
            varying vec2 v_tex_coord;
            '''
    
    `vertex_functions`
        Если задано, строка, содержащая функции, которые будут включены в
        вершинный шейдер.
    
    `fragment_functions`
        Если задано, строка, содержащая функции, которые будут включены во
        фрагментный шейдер.
    
    Другие именованные аргументы должны начинаться с ``vertex_`` или ``fragment_``
    и заканчиваться целочисленным приоритетом. Например, "fragment_200" или "vertex_300". Они
    предоставляют текст, который помещается в соответствующий шейдер с указанным приоритетом,
    причём числа с меньшим приоритетом вставляются перед числами с большим приоритетом.