.. Automatically generated file - do not modify.

.. function:: renpy.check_text_tags(s, check_unclosed=False)
    
    Проверяет текстовые теги в `s` на корректность. Возвращает строку с ошибкой, если
    есть ошибка, или None, если ошибки нет.

.. function:: renpy.error(msg)
    
    Сообщает `msg` (строку) пользователю как ошибку. Это логируется как
    ошибка разбора или анализа (lint), когда это уместно, а в противном случае
    выбрасывается как исключение.

.. function:: renpy.try_compile(where, expr, additional=None)
    
    Пытается скомпилировать выражение и записывает ошибку в lint.txt, если
    это не удаётся.
    
    `where`
        Строка, указывающая место, где находится выражение. Используется для
        генерации сообщения об ошибке вида "Не удалось вычислить `expr`
        в `where`."
    
    `expr`
        Выражение для попытки компиляции.
    
    `additional`
        Если задано, дополнительная строка информации, которая добавляется к
        сообщению об ошибке.

.. function:: renpy.try_eval(where, expr, additional=None)
    
    Пытается вычислить выражение и записывает ошибку в lint.txt, если
    это не удаётся.
    
    `where`
        Строка, указывающая место, где находится выражение. Используется для
        генерации сообщения об ошибке вида "Не удалось вычислить `expr`
        в `where`."
    
    `expr`
        Выражение для попытки вычисления.
    
    `additional`
        Если задано, дополнительная строка информации, которая добавляется к
        сообщению об ошибке.