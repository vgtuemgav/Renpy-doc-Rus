.. Automatically generated file - do not modify.

.. function:: renpy.register_textshader(name, shaders=(), extra_slow_time=0.0, extra_slow_duration=0.0, redraw=None, redraw_when_slow=0.0, include_default=True, adjust_function=None, doc=None, **kwargs)
    
    Это создает textshader и регистрирует его под именем `name`.
    
    Эта функция принимает следующие аргументы:
    
    `name`
        Это имя textshader'а. Оно также используется для регистрации части шейдера
        (shader part) с именем textshader.`name`.
    
    `shaders`
        Части шейдера (shader parts) для применения к тексту. Это может быть строка, или список, или кортеж строк.
        Это должна быть часть шейдера, зарегистрированная с помощью :func:`renpy.register_shader` или этой
        функции. Если часть шейдера начинается с '-', она удаляется из списка
        частей. (Например, '-textshader.typewriter' удалит эту часть.)
    
        Обратите внимание, что части шейдера, зарегистрированные с помощью этой функции, имеют префикс
        textshader., который необходимо указывать при их использовании с этой функцией.
    
    `extra_slow_time`
        Дополнительное время, добавляемое к эффекту медленного времени сверх того, что Ren'Py вычислит из
        текущей скорости символов в секунду. Это полезно для шейдеров, которым
        может потребоваться больше времени для перехода символа, чем стандартное время.
        Если True, шейдер обновляется всегда.
    
    `extra_slow_duration`
        Добавляется к `extra_slow_time`, но умножается на время
        на один символ, чтобы получить дополнительное время, добавляемое к эффекту медленного времени.
        (Время на символ равно 1 / количество символов в секунду.)
    
    `redraw`
        Время в секундах до перерисовки текста после того, как
        весь медленный текст был показан и `extra_slow_time` прошло.
    
    `redraw_when_slow`
        Время в секундах до перерисовки текста во время показа
        медленного текста.
    
    `include_default`
        Если True, при непосредственном использовании этого textshader'а, он будет объединен
        с :var:`config.default_textshader`.
    
    `adjust_function`
        Функция, которая вызывается с объектом и uniforms, передаваемыми
        в textshader в качестве именованных аргументов. Эта
        функция может устанавливать поля `extra_slow_time`, `extra_slow_duration`,
        `redraw` и `redraw_when_slow` объекта.
    
    `doc`
        Строка, содержащая информацию для документации. Это в основном предназначено
        для системы документации Ren'Py.
    
    Именованные аргументы, начинающиеся с ``u_``, передаются в шейдер как uniforms,
    при этом строки, начинающиеся с ``#``, интерпретируются как цвета. Большинство uniforms
    должны начинаться с ``u__``, используя :ref:`локальные переменные шейдера <shader-local-variables>`,
    чтобы предотвратить конфликты с другими шейдерами.
    
    Именованный аргумент `variables` и все именованные аргументы, начинающиеся
    с `fragment_` или `vertex_`, передаются в :func:`renpy.register_shader`,
    которая регистрирует часть шейдера.