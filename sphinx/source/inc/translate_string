.. Automatically generated file - do not modify.

.. function:: renpy.translate_string(s, language=renpy.object.Sentinel('default'))
    
    Немедленно возвращает строку `s`, переведённую на язык `language`. Если `language`
    — `Default`, используется язык, установленный в настройках.
    Строки, заключённые в эту функцию, **не** будут добавлены
    в список переводимых строк. Обратите внимание, что строка может быть
    переведена дважды, если она соответствует строковому переводу в момент
    отображения.