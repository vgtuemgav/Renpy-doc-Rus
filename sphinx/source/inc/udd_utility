.. Automatically generated file - do not modify.

.. function:: renpy.displayable(d: DisplayableLike, scope: dict[str, typing.Any] | None = None) -> Displayable
    
    Принимает `d`, который может быть отображаемым объектом или строкой. Если это
    строка, она преобразуется в отображаемый объект по обычным
    правилам.

.. function:: renpy.end_interaction(value)
    
    Если `value` не None, немедленно завершает текущее взаимодействие, заставляя
    его вернуть `value`. Если `value` равен None, ничего не делает.
    
    Может быть вызван из методов `render` и `event`
    созданного разработчиком отображаемого объекта.

.. function:: renpy.is_pixel_opaque(d, width, height, st, at, x, y)
    
    Возвращает, является ли пиксель в точке (x, y) непрозрачным, когда этот отображаемый
    объект отрисовывается с помощью ``renpy.render(d, width, height, st, at)``.

.. function:: renpy.load_image(im) -> 'renpy.display.render.Render'
    
    Загружает манипулятор изображений `im` с использованием кэша изображений и возвращает объект render.

.. function:: renpy.load_rgba(data: bytes, size: tuple[int, int]) -> Any
    
    Загружает данные изображения `bytes` в текстуру размером `size` и возвращает её.
    
    `data`
        Должен быть байтовым объектом, содержащим данные изображения в порядке RGBA8888.

.. function:: renpy.load_surface(im) -> Surface
    
    Загружает манипулятор изображений `im` с использованием кэша изображений и возвращает поверхность pygame (`Surface`).

.. function:: renpy.map_event(ev, keysym)
    
    Возвращает true, если событие pygame `ev` соответствует `keysym`
    
    `keysym`
        Одно из:
    
        * Имя привязки клавиши в :var:`config.keymap`.
        * Код клавиши (keysym), как описано в разделе :ref:`keymap`.
        * Список, содержащий один или несколько кодов клавиш.

.. function:: renpy.render(d, width, height, /, st, at)
    
    Вызывает отрисовку отображаемого объекта и возвращает объект renpy.Render.
    
    `d`
        Отображаемый объект для отрисовки.
    
    `width`, `height`
        Ширина и высота, доступные для отрисовки отображаемого объекта.
    
    `st`, `at`
        Временные базы показа и анимации.
    
    Объекты `Render`, возвращаемые этой функцией, могут быть кэшированы, и их не
    следует изменять после получения.

.. function:: renpy.timeout(seconds)
    
    Вызывает генерацию события до истечения `seconds` секунд.
    Это гарантирует вызов метода `event` у пользовательского
    отображаемого объекта.