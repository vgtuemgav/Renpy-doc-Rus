.. Automatically generated file - do not modify.

.. function:: renpy.get_autoreload()
    
    Получает флаг автоматической перезагрузки.

.. function:: renpy.reload_script()
    
    Заставляет Ren'Py сохранить игру, перезагрузить скрипт, а затем загрузить
    сохранение.
    
    Эту функцию следует вызывать только во время разработки. Она работает на Windows, macOS
    и Linux, но может не работать на других платформах.

.. function:: renpy.set_autoreload(autoreload)
    
    Устанавливает флаг автоматической перезагрузки, который определяет, будет ли игра
    автоматически перезагружаться после изменений в файлах. Автоматическая перезагрузка не будет
    полностью включена до тех пор, пока игра не будет перезагружена с помощью :func:`renpy.reload_script`.