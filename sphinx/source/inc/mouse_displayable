.. Automatically generated file - do not modify.

.. class:: MouseDisplayable(cursor, x=0, y=0)
    
    Displayable, который является обёрткой для displayable курсора мыши и заставляет
    его двигаться по экрану, когда игрок перемещает мышь.
    
    `cursor`
        Displayable, который используется для отрисовки мыши. Если это None,
        используется аппаратный курсор.
    
    `x`, `y`
        Координаты активной точки (hotspot) относительно верхнего левого
        угла курсора, в виртуальных пикселях.

    .. method:: add(name, cursor, x=0, y=0)
        
        Этот метод добавляет второй курсор, который используется, когда
        отображается мышь с именем `name`. Метод возвращает MouseDisplayable,
        чтобы вызовы этого метода можно было объединять в цепочку.