.. Automatically generated file - do not modify.

.. function:: renpy.register_persistent(field, func)
    
    Регистрирует функцию, которая используется для слияния значений поля persistent-объекта,
    загруженного с диска, со значениями текущего persistent-объекта.
    
    `field`
        Имя поля в persistent-объекте.
    
    `function`
        Функция, которая вызывается с тремя параметрами: `old`, `new` и
        `current`:
    
        `old`
            Значение поля в более старом объекте.
    
        `new`
            Значение поля в более новом объекте.
    
        `current`
            Значение поля в текущем persistent-объекте. Этот параметр
            предоставляется для случаев, когда идентификатор объекта, на который
            ссылается поле, не может измениться.
    
        Ожидается, что функция вернёт новое значение поля в
        persistent-объекте.

.. function:: renpy.save_persistent()
    
    Сохраняет persistent-данные на диск.