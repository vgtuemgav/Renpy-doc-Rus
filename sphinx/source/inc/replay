.. Automatically generated file - do not modify.

.. function:: EndReplay(confirm=True)
    
    Завершает текущий реплей.
    
    `confirm`
        Если true, запрашивает у пользователя подтверждение перед завершением
        реплея.

.. function:: Replay(label, scope={}, locked=None)
    
    Действие, которое запускает `label` как реплей.
    
    `scope`
        Словарь, сопоставляющий имена переменных со значениями. Эти переменные устанавливаются
        при входе в реплей.
    
    `locked`
        Если true, это действие неактивно и ничего не делает при активации.
        Если false, оно будет вести себя как обычно. Если None, оно будет заблокировано, если метка
        не была просмотрена ни в одном прохождении.

.. function:: renpy.call_replay(label, scope={})
    
    Вызывает метку как "воспоминание" (memory).
    
    Аргумент `scope` используется для установки начальных значений переменных в
    контексте "воспоминания".

.. function:: renpy.end_replay()
    
    Если мы находимся в режиме реплея, немедленно его завершает. В противном случае, делает
    ничего.