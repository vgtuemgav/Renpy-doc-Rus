.. Automatically generated file - do not modify.

.. function:: AlphaBlend(control, old, new, alpha=False)
    
    Этот переход использует отображаемый объект `control` (почти всегда какой-либо
    анимированный transform) для перехода от одного отображаемого объекта к другому.
    Transform оценивается. Отображаемый объект `new` используется там, где transform
    непрозрачен, а `old` – там, где он прозрачен.
    
    `alpha`
        Если true, изображение смешивается с тем, что находится за ним. Если false,
        (по умолчанию) изображение непрозрачно и перезаписывает то, что находится за ним.