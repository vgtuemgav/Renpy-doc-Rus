.. Automatically generated file - do not modify.

.. function:: gui.SetPreference(name, value, rebuild=True)
    
    Это Действие (Action) устанавливает настройку GUI с именем `name` в значение `value`.
    
    `rebuild`
        Если true (по умолчанию), вызывается :func:`gui.rebuild`, чтобы
        изменения вступили в силу. Обычно это значение должно быть true, за исключением
        случаев с несколькими действиями gui.SetPreference, где оно
        должно быть False для всех, кроме последнего.
    
    Это очень медленное действие и, вероятно, не подходит для использования
    при наведении курсора на кнопку.

.. function:: gui.TogglePreference(name, a, b, rebuild=True)
    
    Это Действие (Action) переключает настройку GUI с именем `name` между
    значением `a` и значением `b`. Оно считается выбранным, если значение равно
    `a`.
    
    `rebuild`
        Если true (по умолчанию), вызывается :func:`gui.rebuild`, чтобы
        изменения вступили в силу. Обычно это значение должно быть true, за исключением
        случаев с несколькими действиями gui.SetPreference, где оно
        должно быть False для всех, кроме последнего.
    
    Это очень медленное действие и, вероятно, не подходит для использования
    при наведении курсора на кнопку.

.. function:: gui.preference(name, default=...)
    
    Эта функция возвращает значение настройки GUI с
    именем `name`.
    
    `default`
        Если задано, это значение становится значением по умолчанию для настройки
        GUI. Значение по умолчанию должно быть указано при первом
        использовании настройки.